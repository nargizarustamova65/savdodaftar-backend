@extends('admin.layout') @section('content')<h1>Userlar</h1><table><tr><th>ID</th><th>Ism</th><th>Telefon</th><th>Do‘kon</th><th>Holat</th><th></th></tr>@foreach($users as $user)<tr><td>{{ $user->id }}</td><td>{{ $user->name }}</td><td>{{ $user->phone }}</td><td>{{ $user->shop_name }}</td><td>{{ $user->trashed() ? 'Bloklangan' : 'Faol' }}</td><td><a href="{{ route('admin.user', $user) }}">Ko‘rish</a>@if($user->trashed)<form method="post" action="{{ route('admin.unblock', $user->id) }}">@csrf<button>Ochish</button></form>@else<form method="post" action="{{ route('admin.block', $user) }}">@csrf @method('DELETE')<button class="danger">Bloklash</button></form>@endif</td></tr>@endforeach</table>{{ $users->links() }}@endsection
